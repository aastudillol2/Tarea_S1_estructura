def bin_dec(bin):
    pos = 0
    dec = 0 
    
    bin = bin[:: - 1]
    for dig in  bin:
        mult = 2**pos
        dec += int (dig) * mult
        pos +=1
    return dec

bin = input ("Ingresa un numero binario de 4 digitos: ")
dec = bin_dec(bin)
print (dec)
